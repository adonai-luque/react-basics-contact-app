const Header = () => {
  return (
    <div
      className="ui menu"
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "4rem",
      }}
    >
      <h2>Contact Manager</h2>
    </div>
  );
};

export default Header;
